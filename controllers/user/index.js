const register = require('./register');
const login = require('./login');
const logout = require('./logout');
const confirmToken = require('./confirm-token');

module.exports = { register, login, logout, confirmToken };
